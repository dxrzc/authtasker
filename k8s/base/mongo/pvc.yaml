apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pv-claim  
spec:  
  # storageClassName: defaults to Minikube/KIND storage class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 6Gi